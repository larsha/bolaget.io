{{- if .Values.ingress.enabled }}
Provisioned on GKE (Google Cloud)!
{{- else }}
  Provisioned on local cluster!

  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "service=nginx-deployment" -o jsonpath="{.items[0].metadata.name}")
  kubectl port-forward $POD_NAME 3000:80
{{- end }}
